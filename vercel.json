{
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps && cd node_modules/scratch-blocks && npm run prep",
  "outputDirectory": "build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}